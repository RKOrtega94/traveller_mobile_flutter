import 'package:flutter/material.dart';

class AppBannerComponent extends StatelessWidget {
  const AppBannerComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: const Text('Banner'),
    );
  }
}
