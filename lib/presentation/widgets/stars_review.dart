import 'package:flutter/material.dart';

class AppStarsReview extends StatelessWidget {
  const AppStarsReview({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisSize: MainAxisSize.min,
      children: [],
    );
  }
}
